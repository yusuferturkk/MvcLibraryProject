@model List<MvcLibraryProject.Models.TblCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Layout/_AdminLayout.cshtml";
}

<section class="content">
    <div class="card">
        <div class="card-header">
            <h3 class="card-title"><strong>Kategori Listem</strong></h3>
            @using (Html.BeginForm("Index", "Category", FormMethod.Get))
            {
                <div class="card-tools">
                    <div class="input-group input-group-sm float-right" style="width: 220px;">
                        @Html.Editor("key")

                        <div class="input-group-append">
                            <button type="submit" class="btn btn-default"><i class="fas fa-search"></i></button>
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="card-body p-0">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th style="width:10%">#</th>
                        <th>Kategori Adı</th>
                        <th style="width:10%">Sil</th>
                        <th style="width:10%">Güncelle</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.CategoryId</td>
                            <td>@item.CategoryName</td>
                            <td><a href="/Category/DeleteCategory/@item.CategoryId/" class="btn btn-danger"><i class="fas fa-trash"></i> Sil</a></td>
                            <td><a href="/Category/UpdateCategory/@item.CategoryId/" class="btn btn-success"><i class="fas fa-edit"></i> Güncelle</a></td>

                        </tr>
                    }
                </tbody>
            </table>

            <div class="card-footer">
                <a href="/Category/AddCategory/" class="btn btn-primary"><i class="fas fa-bookmark"></i> Kategori Ekle</a>
            </div>
        </div>
    </div>
</section>